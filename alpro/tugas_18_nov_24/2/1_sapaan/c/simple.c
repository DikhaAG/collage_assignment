#include <stdio.h>
int main() {
    char nama[20], kota[20];
    printf("syp nm-mu? ");
    scanf("%s", nama);
    printf("dikota ap km skrng? ");
    scanf("%s", kota);
    printf("senang brtmu dg mu, %s, dikota %s\n", nama, kota);
    return 0;
}